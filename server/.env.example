# PORT the server listens on (optional)
PORT=5174

# OpenAI API key (recommended) — set this to your OpenAI API key or another provider key
OPENAI_API_KEY=sk-xxxx

# Model to use (optional)
OPENAI_MODEL=gpt-3.5-turbo

# If you want the proxy to forward requests to your own AI API, set the URL below
# Example: http://127.0.0.1:5000/api/chatbot
MY_AI_API_URL=http://127.0.0.1:5000/api/chatbo
# Optional: key to include when forwarding to your API. If value starts with 'Bearer '
# it will be forwarded as Authorization header; otherwise it will be sent as x-api-key.
MY_AI_API_KEY=
